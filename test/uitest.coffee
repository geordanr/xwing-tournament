# Run this with mocha-phantomjs
describe "X-Wing Tournament Helper UI", ->
    describe "YASB User", ->
        it "should be able to associate a YASB account with a Tournament Helper account", ->
            assert false, 'not implemented'

        it "should be able to submit a list from the YASB interface", ->
            assert false, 'not implemented'

